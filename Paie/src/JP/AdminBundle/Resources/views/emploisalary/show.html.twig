{% extends "JP\AdminBundle::layout.html.twig" %}

{% block body %}
    <h1>Emploisalary</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emploiSalary.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ emploiSalary.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_emploi_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_emploi_edit', { 'id': emploiSalary.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
