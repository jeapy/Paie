{% extends "JP\AdminBundle::layout.html.twig" %}

{% block body %}
    <h1>Statussalary</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statusSalary.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ statusSalary.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_status_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_status_edit', { 'id': statusSalary.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
